DEEPSEEK_API_KEY=
PERPLEXITY_API_KEY=

# Access key required to reach synfc_engine/server.py's /api/* endpoints
# (the Analyst and Agent Interface pages ask for this). Generate one with:
#   python -c "import secrets; print(secrets.token_urlsafe(12))"
# Never commit the real value -- .env is gitignored, this file is the template.
SYNFC_ACCESS_KEY=
